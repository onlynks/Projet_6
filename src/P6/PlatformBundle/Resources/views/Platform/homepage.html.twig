{% extends "layout.html.twig" %}

{% block header %}{{ parent() }} - Homepage{% endblock %}

{% block carousel %}
    <!--
    <div id="carouselExampleControls" class="background-carousel slide" data-ride="carousel">
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100" src="{{ asset('Images/fond.jpg') }}" alt="First slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" src="{{ asset('Images/fond2.jpg') }}" alt="Second slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" src="{{ asset('Images/fond3.jpg') }}" alt="Third slide">
            </div>
        </div>
    </div>
-->
{% endblock %}

{% block body %}

    <div class="container">

        <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>

        <div class="row">

            {% for category in listCategories %}
                <div class="col-md-4">

                    <div class="card mb-4 box-shadow">

                            <h2>{{ category.name }}</h2>
                                <div class="card-body">
                                    {% for trick in category.trick %}
                                        {% if loop.index<4 %}
                                        <p class="card-text">
                                            <a href="{{ path('p6_onetrick', {'id': trick.id}) }}">{{ trick.name }}</a>
                                            {{ trick.description }}
                                        </p>
                                        {% endif %}
                                    {% endfor %}
                                    <p><a class="btn btn-secondary" href="#" role="button">View details »</a></p>
                                </div>

                    </div>

                </div>

            {% endfor %}
        </div>

    </div>


{% endblock %}
